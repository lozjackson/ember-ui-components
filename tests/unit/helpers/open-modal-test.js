import openModal from 'dummy/helpers/open-modal';
import { module, test } from 'qunit';

module('Unit | Helper | open modal');

test('it works', function(assert) {
  assert.equal(typeof openModal, 'function');
});
