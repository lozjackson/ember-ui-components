{{#if dialog.title}}
  <h1>{{dialog.title}}</h1>
{{/if}}
{{#if dialog.body}}
  <p>{{dialog.body}}</p>
{{/if}}
<div class="euic-modal-button-container">
  <button class="euic-ok-button" {{action "ok"}}>OK</button>
</div>
