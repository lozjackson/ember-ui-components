{{yield to="inverse"}}
{{#if showContextMenu}}
  {{#ember-wormhole to=destinationElementId renderInPlace=(unless destinationElementId true)}}
    {{#uic-content-mask active=true click=(action "closeContextMenu") class="uic-menu-overlay"}}
      {{#uic-context-menu-container closeContextMenu=(action "closeContextMenu")}}
        {{yield contextMenuParams}}
      {{/uic-context-menu-container}}
    {{/uic-content-mask}}
  {{/ember-wormhole}}
{{/if}}
