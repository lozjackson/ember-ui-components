{{#if dialog.title}}
  <h2>{{dialog.title}}</h2>
{{/if}}
{{#if dialog.body}}
  <p>{{dialog.body}}</p>
{{/if}}
<div class="euic-modal-button-container">
  <button class="euic-cancel-button" {{action "cancel"}}>Cancel</button>
  <button class="euic-ok-button" {{action "confirm"}}>OK</button>
</div>
