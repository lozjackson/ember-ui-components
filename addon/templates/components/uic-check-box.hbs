{{#if _checkboxId}}
  {{input checked=checked id=_checkboxId disabled=disabled type="checkbox"}}
  <label for={{_checkboxId}}></label>
{{else}}
  {{input checked=checked disabled=disabled type="checkbox"}}
{{/if}}
