<span class="ellipsis">{{title}}</span>
<div>
  {{svg-triangle direction="right" size="8"}}
  {{#if showMenu}}
    {{#uic-menu-container}}
      {{#if hasBlock}}
        {{yield}}
      {{/if}}
    {{/uic-menu-container}}
  {{/if}}
</div>
